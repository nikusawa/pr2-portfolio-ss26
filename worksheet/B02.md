# Lösungen Übungsblatt B02

Das zugehörige Übungsblatt: https://github.com/Programmiermethoden-CampusMinden/Prog2-Lecture-S26/blob/master/homework/b02.md

## 1. Git-Spiel

### 1.1
Auf `master` die Datei `hero.md` editieren (z. B. Leerzeile hinzufügen) und committen:
    
    echo "" >> hero.md
    git add hero.md
    git commit -m "Neue Leerzeile in hero.md auf master"

Anschließend end in master mergen:

    git merge origin/end

### 1.2

`questlog.md` auf `master` ändern (z. B. einen Satz am Anfang des letzten Absatzes einfügen):

    sed -i '1s/^/Neuer Satz auf master. /' questlog.md
    git add questlog.md
    git commit -m "Änderung am Anfang von questlog.md auf master"

`sed` command gibt es nur auf Linux.

    git merge origin/end

### 1.3

`questlog.md` erneut bearbeiten, diesmal exakt die Stelle, die auch in `end` geändert wurde (z. B. den letzten Satz):

    sed -i 's/letzte Etappe/allerletzte Etappe/' questlog.md
    git add questlog.md
    git commit -m "Änderung am Ende von questlog.md auf master"

### 1.4

end auf master rebasieren:

    git checkout end
    git rebase master

Dann zurück zu master und mergen:

    git checkout master
    git merge end

## 2. Katzen-Café

### 2.1 Gradle

Die build.gradle Datei:

    plugins {
    id 'java'
    id 'application'
    id 'com.diffplug.spotless' version '8.4.0'
    }

    repositories {
        mavenCentral()
    }

    dependencies {
        // JUnit 6 (aktuellste stabile Version)
        testImplementation 'org.junit.jupiter:junit-jupiter:6.0.3'
        testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    }

    application {
        mainClass = 'catcafe.Main'
    }

    java {
        sourceCompatibility = JavaVersion.VERSION_25
        targetCompatibility = JavaVersion.VERSION_25
    }

    test {
        useJUnitPlatform()
    }

    spotless {
        java {
            // Google Java Format mit 4 Leerzeichen Einrückung (AOSP-Stil)
            googleJavaFormat().aosp()
            // Zeilenlänge auf 120 Zeichen begrenzen, um lange Strings umzubrechen
            googleJavaFormat().maxColumn(120)
            // Importe automatisch ordnen
            importOrder('java', 'javax', 'org', 'com', 'catcafe')
            removeUnusedImports()
        }
    }

### 2.2 JUnit

    @BeforeEach
    void setUp() {
        cafe = new CatCafe();
        luna = new FelineOverLord("Luna", 2);
        felix = new FelineOverLord("Felix", 3);
        simba = new FelineOverLord("Simba", 3);
        emma = new FelineOverLord("Emma", 5);
    }

    // 1. Neues Café ist leer
    @Test
    void givenNewCafe_whenCounted_thenZero() {
        // given (implizit: setUp)
        // when
        long count = cafe.getCatCount();
        // then
        assertEquals(0, count);
    }

    // 2. Nach dem Hinzufügen einer Katze steigt Zähler
    @Test
    void givenEmptyCafe_whenAddCat_thenCountIncreases() {
        // when
        cafe.addCat(luna);
        // then
        assertEquals(1, cafe.getCatCount());
    }

    // 3. Keine doppelten Einträge
    @Test
    void givenCatAlreadyPresent_whenAddSameCat_thenCountDoesNotChange() {
        cafe.addCat(luna);
        cafe.addCat(luna);
        assertEquals(1, cafe.getCatCount());
    }

    // 4. Katze nach Namen finden
    @Test
    void givenCats_whenGetCatByName_thenReturnCorrectCat() {
        cafe.addCat(luna);
        cafe.addCat(simba);
        FelineOverLord found = cafe.getCatByName("Simba");
        assertNotNull(found);
        assertEquals("Simba", found.name());
    }

    // 5. Suche nach nicht existierendem Namen liefert null
    @Test
    void givenCats_whenGetCatByNameWithUnknown_thenReturnNull() {
        cafe.addCat(luna);
        assertNull(cafe.getCatByName("Garfield"));
    }

    // 6. Namenssuche mit null als Argument
    @Test
    void givenCats_whenGetCatByNameWithNull_thenReturnNull() {
        cafe.addCat(luna);
        assertNull(cafe.getCatByName(null));
    }

    // 7. Katze nach Gewicht finden (innerhalb der Grenzen)
    @Test
    void givenCats_whenGetCatByWeightInRange_thenReturnCorrectCat() {
        cafe.addCat(luna); // Gewicht 2
        cafe.addCat(emma);    // Gewicht 5
        FelineOverLord found = cafe.getCatByWeight(3, 6);
        assertNotNull(found);
        // emma (5) liegt im Intervall [3,6)
        assertEquals("Emma", found.name());
    }

    // 8. Gewichtssuche ohne Treffer
    @Test
    void givenCats_whenGetCatByWeightOutOfRange_thenReturnNull() {
        cafe.addCat(luna); // 2
        assertNull(cafe.getCatByWeight(5, 10));
    }

    // 9. Gewichtssuche mit ungültigen Grenzen
    @Test
    void givenInvalidWeightBounds_thenReturnNull() {
        cafe.addCat(luna);
        assertNull(cafe.getCatByWeight(5, 3)); // min > max
        assertNull(cafe.getCatByWeight(-1, 3)); // min negativ
    }

Die @BeforeEach-Funktion passiert vor jedem Test und macht alles übersichtlicher und platzsparender, weil die Initialisierung für ziemlich alle Tests gleich ist. 

## 3. Remotes und CI-Pipeline

Die `.yaml` Datei:

    name: Java CI with Gradle

    on:
    push:
        branches: [ "master" ]
    pull_request:
        branches: [ "master" ]

    jobs:
        build:
            runs-on: ubuntu-latest

            steps:
                - name: Repository auschecken
                uses: actions/checkout@v4

                - name: JDK 25 einrichten
                uses: actions/setup-java@v4
                with:
                    java-version: '25'
                    distribution: 'temurin'


                - name: Gradlew Berechtigungen setzen
                run: chmod +x gradlew

                - name: Gradle set up
                uses: gradle/actions/setup-gradle@v3

                - name: Projekt bauen
                run: ./gradlew build

                - name: JUnit-Tests ausführen
                run: ./gradlew test

                - name: Code-Formatierung prüfen
                run: ./gradlew spotlessCheck

Diese CI-Datei basierte auf das "Java with Gradle" Preset, welches ich dann erweitert habe. Aktuell funktioniert sie noch nicht, es kommt zu Fehler beim Bau des Projektes.